require(['../libs/config'], () => {
    require([
        'jquery',
        'css',
        'bootstrap',
        'jquery-ui',
        'jquery.validate',
        '_swiper'
    ], ($, Swiper) => {
        $(function () {
            // $.ajax({
            //     type:'get',
            //     url:'http://localhost:9528/api/goodlist'
            // }).done(function(data){
            //     console.log(data);
            // })
        })
    })
})